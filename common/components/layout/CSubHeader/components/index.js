import { styled, Box } from '@mui/material';

const StyledHeaderBox = styled(Box)(({ theme }) => ({
	backgroundColor: theme.palette.background.default,
	padding: '70px 0',
}));

export { StyledHeaderBox };
